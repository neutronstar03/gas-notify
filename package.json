{
  "name": "gas-notify",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "bin": {
    "gas-notify": "./src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "dev": "bun --watch run src/cli.ts",
    "check": "bunx tsc --noEmit",
    "validate:rpcs": "bun run scripts/validate-rpcs.ts",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "toasted-notifier": "^10.1.0"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^7.7.2",
    "@types/bun": "latest",
    "eslint": "^10.0.3",
    "typescript": "^5.9.2"
  }
}
