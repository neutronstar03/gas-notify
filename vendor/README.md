Place `SnoreToast.exe` here, or point `snoreToastPath` at another local copy.

You can provision it with:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\provision-snoretoast.ps1
```
